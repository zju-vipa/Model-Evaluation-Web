"""
Django settings for ME project.

Generated by 'django-admin startproject' using Django 4.2.3.

For more information on this file, see
https://docs.djangoproject.com/en/4.2/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/4.2/ref/settings/
"""

from pathlib import Path
import os, datetime

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent

# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/4.2/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'django-insecure-wwt6rr1vkr)aijw&5f=pwmx5n(-5mw^*x+6##)(ric$@0h5!u-'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = False
STATIC_URL = "/disk1/wjx/vipa_model_evaluation_server/static"
STATIC_ROOT  = "/disk1/wjx/vipa_model_evaluation_server/static" #指定样式收集目录

ALLOWED_HOSTS = ['*']

# Application definition
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'rest_framework',
    # apps created manually
    'datasets.apps.DatasetsConfig',
    'cv_models.apps.CvModelsConfig',
    'tasks.apps.TasksConfig',
    'users.apps.UsersConfig',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

# JWT配置
REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': (
        'rest_framework_simplejwt.authentication.JWTAuthentication',
    ),
}

SIMPLE_JWT = {
    'ACCESS_TOKEN_LIFETIME': datetime.timedelta(days=7),  # 访问令牌的有效期
    'REFRESH_TOKEN_LIFETIME': datetime.timedelta(seconds=0),  # 禁用刷新令牌
    "SLIDING_TOKEN_LIFETIME": datetime.timedelta(seconds=0),  # 禁用滑动令牌
    "SLIDING_TOKEN_REFRESH_LIFETIME": datetime.timedelta(seconds=0),  # 禁用滑动令牌
    'ALGORITHM': 'HS256',
    'SIGNING_KEY': 'test-signing-key',
}

ROOT_URLCONF = 'ME.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [os.path.join(BASE_DIR, 'templates')],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'ME.wsgi.application'

# Database
# https://docs.djangoproject.com/en/4.2/ref/settings/#databases

# sqlite3 配置
# DATABASES = {
#     'default': {
#         'ENGINE': 'django.db.backends.sqlite3',
#         'NAME': BASE_DIR / 'db.sqlite',
#     }
# }

DATABASES = {
    'default': {
        'OPTIONS': {'charset': 'utf8mb4', },
        'ENGINE': 'django.db.backends.mysql',  #更改为mysql
        'NAME': 'evaluation',  #数据库名
        'USER': 'root',      #数据库的用户名
        'PASSWORD': '123456',   #密码
        'HOST': '127.0.0.1',    #本地地址，不是远程操作无需填写地址
        'PORT': '3336', #端口号
    }
}

# Password validation
# https://docs.djangoproject.com/en/4.2/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]

# Internationalization
# https://docs.djangoproject.com/en/4.2/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'Asia/Shanghai'

USE_I18N = True

USE_TZ = True

# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/4.2/howto/static-files/

STATIC_URL = 'static/'

# Default primary key field type
# https://docs.djangoproject.com/en/4.2/ref/settings/#default-auto-field

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

LOGIN_REDIRECT_URL = '/'

AUTH_USER_MODEL = 'users.User'

EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'

'''
The following are user-defined configuration items that can be called using [settings. *name*] in the code
'''
UNIVERSAL_RELATIVE_PATH = '/home/xjb/our_user/'
FILE_STORAGE_ROOT = UNIVERSAL_RELATIVE_PATH

# 用户上传文件的存储路径
MEDIA_ROOT = FILE_STORAGE_ROOT

BASIC_IP_PORT = '192.168.0.0:8000'
EVALUATE_IP_PORT = "http://127.0.0.1:3308"
DEFAULT_CHARSET = 'utf-8'
